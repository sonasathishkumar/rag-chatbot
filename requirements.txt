# Core app
streamlit>=1.28.0
python-dotenv>=1.0.0

# PDF processing
pdfplumber>=0.9.0
PyPDF2>=3.0.0
reportlab>=4.0.0
fpdf2>=2.7.0

# Data / viz
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
plotly>=5.17.0

# Auth
bcrypt==4.0.1

# LLM — Groq API client (https://pypi.org/project/groq/)
groq>=0.9.0

# Embeddings + vector search
# Pinned as the last known compatible set for Python 3.11.
# Do NOT loosen these pins — they resolve the transformers/sentence-transformers
# version conflict (transformers.integrations → CodeCarbonCallback chain).
transformers==4.41.2
tokenizers==0.19.1
huggingface-hub==0.23.4
sentence-transformers==2.7.0
faiss-cpu>=1.7.4